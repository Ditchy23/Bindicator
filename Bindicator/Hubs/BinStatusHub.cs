using Microsoft.AspNetCore.SignalR;

namespace Bindicator.Hubs
{
    public class BinStatusHub : Hub
    {
        // You can add methods for client->server calls here if you want,
        // but for now, just inheriting Hub is enough!
    }
}
